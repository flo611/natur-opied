{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

  <div class="bg-black w-full flex flex-col items-center justify-center h-screen "> 
    <h1 class="font-bold text-4xl text-white">Register</h1>
{# 
    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn">Register</button> #}


    {# {{ form_errors(registrationForm) }} #}
    {{ form_start(registrationForm) }}
    <div class="flex flex-col px-80  border-red-500 rounded-xl w-full border-4 my-6">

        <label for="registration_form_email" class="text-white py-4 px-60 ">Votre email : </label>
        {{ form_widget(registrationForm.email, { 'attr' : {'label': 'Email', 'class': 'text-black '}}) }}

        <label for="registration_form_plainPassword" class="text-white px-60  py-4">Votre mot de passe : </label>
        {{ form_widget(registrationForm.plainPassword, { 'attr' : {'label': 'Password', 'class': 'text-black'}}) }}

        <label for="registration_form_agreeTerms" class="text-white flex justify-center  py-4">Confirmer</label>
        {{ form_widget(registrationForm.agreeTerms, { 'attr' : {'label': 'Accept', 'class': 'text-white'}}) }}

    <div class=" flex justify-center border-4 mx-auto px-4 my-4 border-black bg-white text-black rounded-xl hover:bg-red-500 hover:text-black">
        <button type="submit" class="btn">Register</button>
        </div>
    </div>
    
     
        {# {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}
        
          <button type="submit" class="btn">Register</button>
     #}
    {{ form_end(registrationForm) }}
    </div>
{% endblock %}
